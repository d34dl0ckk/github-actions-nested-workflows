name: reusable workflow B
on:
  workflow_call:
    inputs:
      value:
        type: string
        required: true

jobs:
  debug-output-B:
    name: debug output
    runs-on: self-hosted
    steps:
      - run: |
          echo '${{ inputs.value }}'
